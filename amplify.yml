version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 16  # Specify Node.js version (adjust if needed)
        - nvm use 16
        - npm install -g pnpm
        - pnpm install
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: dist  # or 'build', depending on your setup
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*